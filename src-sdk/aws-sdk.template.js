(function () {
  var window = window || {};

  // awk-sdk.js placeholder --                 begin
  //=include aws-sdk.js
  // awk-sdk.js placeholder --                 end

  this.AWS = window.AWS;
}.call(this));
